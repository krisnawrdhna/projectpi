<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.projectpi">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <activity android:name=".splash_screen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".kuala2" />
        <activity android:name=".mapbali" />
        <activity android:name=".jeju" />
        <activity android:name=".seoul" />
        <activity android:name=".shanghai" />
        <activity android:name=".beijing" />
        <activity android:name=".osaka" />
        <activity android:name=".tokyo" />
        <activity android:name=".genting" />
        <activity android:name=".Tujuan.gardenby" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity android:name=".mapjakarta" />
        <activity android:name=".bali" />
        <activity android:name=".gardenby" />
        <activity android:name=".marinabay" />
        <activity android:name=".jakarta" />
        <activity android:name=".indonesia" />
        <activity android:name=".Tujuan.korea" />
        <activity android:name=".Tujuan.jepang" />
        <activity android:name=".Tujuan.cina" />
        <activity android:name=".Tujuan.malaysia" />
        <activity android:name=".Tujuan.singapur" />
        <activity android:name=".MainActivity"></activity>
    </application>

</manifest>